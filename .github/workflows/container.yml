name: CI
on:
  push:

jobs:
  container-test-job:
    runs-on: jph-self-hosted
    container:
      image: localhost:5000/cfsbuildenv-ubuntu22:test

    steps:
      - name: Say Hello
        run: echo "Hello, world"
 
